
const Log = {

    e : (message: string|any)=>{

        console.log(message);
    },

    i : (message: string|any)=>{

        console.log(message);
    },

    /**
     * fata error logging to file
     */
    fe : (message: string|any)=>{

        console.log(message);
    },
}

export default Log;